<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:views="clr-namespace:LocalWallet.Views"
       x:Class="LocalWallet.AppShell"
       Shell.FlyoutBehavior="Disabled"
       Shell.TabBarIsVisible="False"
       Title="LocalWallet">

    <!-- Five routed pages; Shell's native bottom bar is hidden and our
         Liquid-Glass AppTabBar floats over each page via AppPageTemplate. -->
    <TabBar>
        <ShellContent Title="Главная"
                      ContentTemplate="{DataTemplate views:DashboardPage}"
                      Route="dashboard" />
        <ShellContent Title="Операции"
                      ContentTemplate="{DataTemplate views:TransactionsPage}"
                      Route="transactions" />
        <ShellContent Title="Валюты"
                      ContentTemplate="{DataTemplate views:MultiCurrencyPage}"
                      Route="multicurrency" />
        <ShellContent Title="Чат"
                      ContentTemplate="{DataTemplate views:ChatHubPage}"
                      Route="chat" />
        <ShellContent Title="Настройки"
                      ContentTemplate="{DataTemplate views:SettingsPage}"
                      Route="settings" />
    </TabBar>

</Shell>
